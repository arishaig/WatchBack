services:
  watchback:
    build: .
    container_name: watchback
    env_file:
      - .env
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Los_Angeles
    volumes:
      - ./config:/config
    ports:
      - "8080:8000"
    restart: unless-stopped
