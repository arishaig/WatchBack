[pytest]
testpaths = tests
filterwarnings =
    ignore:coroutine 'restart_server.<locals>._do_exit' was never awaited:RuntimeWarning
markers =
    a11y: accessibility tests (require Playwright browsers to be installed)