[pytest]
testpaths = tests
addopts = --cov=main --cov-report=term-missing --cov-report=html:htmlcov -p no:unraisableexception
markers =
    a11y: accessibility tests (require Playwright browsers to be installed)
